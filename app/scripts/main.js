$(document).ready(function($) {  // initialize controller  var controller = new ScrollMagic();  // custom tweens  //var tween1 = TweenMax.to('#intro',   // assign handler and add to controller  var pinPolaroid = new ScrollScene({triggerElement: '#trigger_polaroid_pin', duration:  1000})                  .setPin('#header')                  .addTo(controller);  // scene.addIndicators();});